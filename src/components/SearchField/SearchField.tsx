import React from 'react';

type Props = {
    onSearch: () => void;
};

export const SearchField = (props: Props) => {
    const [phrase, setPhrase] = React.useState('');

    // const onSubmit = () => {
    //     props.method(...args)
    // }
// return (div)

    // return (
        
    // )
    return (<h1>Searh</h1>)
};